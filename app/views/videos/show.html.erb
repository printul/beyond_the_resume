<h1>Your video</h1>
<p><%= @video.title %></p>
<p><%= @video.url %></p>
<p><%= @video.description %></p>

<p><%= link_to "Go to all videos", videos_path %></p>
<p><%= link_to "Delete video", video_path(@video), method: :delete %></p>

<ziggeoplayer ziggeo-video='<%= @video.url %>'
    ziggeo-width=640
    ziggeo-height=480>
</ziggeoplayer>

<% url = "www.google.com" %>

<%#= image_tag(qrcode_path(data: url), id: 'qr_code') %>

<%#= link_to('download', qrcode_path(data: url), download: 'download' ) %>
